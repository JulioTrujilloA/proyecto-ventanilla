# Fotos subidas por usuarios (no versionar contenido de uploads)
uploads/*
!uploads/.gitkeep

# Dependencias de Composer (instalar con: php composer.phar install)
/vendor/
composer.phar

# Temporales / sistema
*.log
Thumbs.db
.DS_Store
